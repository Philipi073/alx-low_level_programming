#include<stdio.h>
/**
 * main - Entry point
 * Description: outputs numbers
 * Return:0
 */
int main(void)
{
	int digit;

	for (digit = 0; digit < 10; digit++)
	{
		printf("%d", digit);
	}
	printf("\n");
	return (0);
}

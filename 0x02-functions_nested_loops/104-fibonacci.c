#include <stdio.h>
#define largest 10000000000
/**
 * main - Entry point
 * Description: print the first 98 fib
 * Return 0
 */
int main(void)
{
	unsigned long int fri = 0, bk =1, fr2 = 0, bk2 =2;
	unsigned long int hold1, hold2, hold3;
	int count;

	printf("%lu, %lu,",bk1, bk2);
	for (count = 2; count < 98; count++)
	{
		if (bk1 + bk2 > LARGEST || fr2 > 0 || fr1 > 0)
		{
			hold1 = (bk1 +bk2)
		}
	}
}

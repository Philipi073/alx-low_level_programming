#include "main.h"
/**
 * print_times_table - prints n timetable starting with zero
 * Description:outputs time table
 * @n: int type number
 */
void print_times_table(int n);
{
	int x = 0, y, z;

	if (n > 15 || n < 0)
		return;
	while (x <= n)
	{
		for y
	}
}
